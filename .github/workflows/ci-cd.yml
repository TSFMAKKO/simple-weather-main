name: CI  # workflow 名稱，可自訂

on:
  push:
    branches: [main]  # 監聽 main 分支
  pull_request:       # 監聽 PR

jobs:
  ci:                  # job ID，可自訂
    runs-on: ubuntu-latest  # 固定語法，指定 runner

    steps:              # 固定語法
      - name: Checkout repository  # step 顯示名稱，可自訂
        uses: actions/checkout@v6  # 固定語法，用 action

      - name: Check repository      # step 顯示名稱，可自訂
        run: |                     # 固定語法，用 shell 指令
          echo "📋 作業系統資訊"
          cat /etc/os-release

          echo ""
          echo "📂 目前路徑"
          pwd

          echo ""
          echo "📁 檔案列表"
          ls -al

          echo ""
          echo "🟢 Node.js 版本"
          node -v

          echo ""
          echo "🌿 分支資訊"
          git branch -vv